# Sculpture in Data — Claude Code Adapter

@AGENTS.md

This file intentionally imports the vendor-neutral repository guide. Follow the read order in `AGENTS.md`, including `.windsurfrules` for current implementation values and any nearer scoped instructions.

Do not add Claude-only project policy here. Put durable policy in `AGENTS.md`, product outcomes in `docs/PROJECT_CHARTER.md`, sequencing in `docs/ROADMAP.md`, and implementation values in `.windsurfrules`.
